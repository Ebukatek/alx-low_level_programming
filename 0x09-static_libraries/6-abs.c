#include "main.h"

/**
 * _abs - Computes the absolute value of an integer.
 * @c: The number to be computed
 * Return: -c or c;
 */

int _abs(int c)
{
	if (c < 0)
		return (-c);
	else if (c >= 0)
	{
		return (c);
	}
}
