#include <stdio.h>

/**
 * main - prints program name
 * @argc: counts the argument
 * @argv: the argument vector
 * Return: 0
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
